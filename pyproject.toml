[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "multioutcome-cvae"             # pip / distribution name
version = "0.3.0"
description = "Conditional Variational Autoencoder for multivariate Bernoulli, categorical, Gaussian, and Poisson outcomes"
readme = "README.md"
requires-python = ">=3.9"

authors = [
  { name = "Jarrod Dalton" }
]

license = { text = "MIT" }

keywords = [
  "cvae",
  "variational-autoencoder",
  "multivariate",
  "categorical",
  "simulation",
  "generative-models",
  "machine-learning",
  "deep-learning"
]

classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Intended Audience :: Science/Research",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

# Runtime dependencies
dependencies = [
  "numpy>=1.20",
  "torch>=2.0",
]

[project.optional-dependencies]
plot = ["matplotlib>=3.3"]
tuning = ["hyperopt>=0.2"]
mlflow = ["mlflow>=2.0"]
test = ["pytest>=7", "matplotlib>=3.3"]
all = [
  "matplotlib>=3.3",
  "hyperopt>=0.2",
  "mlflow>=2.0",
]

# Tell setuptools where packages live
[tool.setuptools]
package-dir = {"" = "python"}

[tool.setuptools.packages.find]
where = ["python"]

[tool.pytest.ini_options]
# The reproducible validation runner intentionally lives at repository root
# rather than in the shipped inference package.
pythonpath = ["."]
